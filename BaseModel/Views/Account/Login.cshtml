@model RegisterViewModel
@{
    ViewData["Title"] = "Login";
}

<div class="containerAuth m-auto mt-3">
    <div class="form-box login">
        <form method="post" asp-controller="Account" asp-action="Login">
            <h1>Login</h1>
            <div class="input-box">
                <input asp-for=@Model.Username type="text" placeholder="Username" required>
                <i class="bi bi-person-fill"></i>
            </div>
            <div class="input-box">
                <input asp-for="@Model.Password" type="password" placeholder="Password" required>
                <i class="bi bi-lock-fill"></i>
            </div>
            <div class="forgot-link">
                <a href="#">Forgot Password?</a>
            </div>
            <button type="submit" class="btn-custom">Login</button>
        </form>
    </div>


    <div class="toggle-box">
        <div class="toggle-panel toggle-left">
            <h1>Hello, Welcome!</h1>
            <p>Don't have an account?</p>
            <p>Ask your manage to handle it!</p>
        </div>
    </div>
</div>
